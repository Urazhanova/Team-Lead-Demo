# План адаптации Team Lead Academy для мобильной версии

## 📱 Обзор

План включает оптимизацию всех экранов курса для мобильных устройств (320px - 480px).

**Целевые устройства:**
- Смартфоны: 320px - 480px (iPhone SE, старые Android)
- Фаблеты: 480px - 768px (iPad Mini, большие смартфоны)
- Планшеты: 768px - 1024px (iPad)
- Десктоп: 1024px+

---

## 🎯 Фаза 1: Аудит текущего состояния

### Задачи:
1. **Протестировать все экраны на мобильных разрешениях**
   - Используя DevTools (F12 → Toggle device toolbar)
   - Проверить 320px, 375px (iPhone), 414px, 480px
   - Проверить ориентацию (Portrait и Landscape)

2. **Определить проблемные элементы**
   - Текст, который переносится неправильно
   - Изображения, которые обрезаются
   - Кнопки, которые слишком маленькие
   - Промежутки, которые неправильные

3. **Проверить производительность**
   - Скорость загрузки на медленном интернете (Throttle: Slow 3G)
   - Размер изображений
   - Количество запросов

### Файлы для проверки:
- ✓ index.html (viewport meta, структура)
- ✓ css/layouts.css (контейнеры, сетка)
- ✓ css/components.css (компоненты)
- ✓ js/screen-renderer.js (структура экранов)

---

## 🎨 Фаза 2: Адаптация CSS

### 2.1 Основные контейнеры (layouts.css)

```css
/* Мобильная версия контейнера */
@media (max-width: 480px) {
  .container {
    padding: var(--space-md);  /* 16px вместо 24px */
    min-height: calc(100vh - var(--header-height) - var(--space-lg));
  }
  
  .card {
    border-radius: var(--radius-md);  /* Меньше скругление */
    padding: var(--space-lg);  /* 24px вместо 32px */
    margin-bottom: var(--space-md);
  }
}
```

### 2.2 Текст и заголовки

```css
@media (max-width: 480px) {
  h1 {
    font-size: 24px;  /* Было 32px */
    line-height: 1.3;
  }
  
  h2 {
    font-size: 20px;  /* Было 28px */
  }
  
  h3 {
    font-size: 16px;  /* Было 20px */
  }
  
  p, span {
    font-size: 14px;  /* Было 16px */
  }
}
```

### 2.3 Макет контента (две колонки → одна колонка)

```css
@media (max-width: 768px) {
  .content-left,
  .content-right {
    width: 100%;
  }
  
  .card-content {
    flex-direction: column;
  }
  
  .card-content .content-left {
    margin-right: 0;
    margin-bottom: var(--space-lg);
  }
}
```

### 2.4 Изображения

```css
@media (max-width: 480px) {
  img {
    max-width: 100%;
    height: auto;
  }
  
  .character-image {
    width: 100%;
    max-width: 300px;
    height: auto;
  }
  
  /* Скрыть большие изображения на мобильной версии */
  .image-large-only {
    display: none;
  }
}
```

### 2.5 Кнопки

```css
@media (max-width: 480px) {
  button {
    padding: var(--space-md) var(--space-lg);  /* Больше клик-зона */
    min-height: 44px;  /* Apple's recommendation */
    font-size: 16px;   /* Предотвратить zoom на iOS */
  }
  
  .button-group {
    flex-direction: column;
    gap: var(--space-md);
  }
  
  .button-group button {
    width: 100%;
  }
}
```

### 2.6 Header и навигация

```css
@media (max-width: 768px) {
  .header {
    padding: var(--space-sm) var(--space-md);
    height: auto;
    flex-wrap: wrap;
  }
  
  .header-progress {
    display: none;  /* Скрыть на мобильной */
  }
  
  .header-logo {
    font-size: 16px;
    flex-basis: 100%;
  }
}
```

### 2.7 Гамбургер меню

```css
@media (max-width: 768px) {
  .hamburger-btn {
    display: flex;
  }
  
  .hamburger-btn.active span:nth-child(1) {
    transform: rotate(45deg) translateY(10px);
  }
  
  .hamburger-btn.active span:nth-child(3) {
    transform: rotate(-45deg) translateY(-10px);
  }
}

@media (min-width: 769px) {
  .hamburger-btn {
    display: none;
  }
}
```

---

## 📸 Фаза 3: Адаптация типов экранов

### 3.1 Intro Screen (Введение)

**Проблемы на мобильной:**
- Две колонки не умещаются
- Изображение слишком большое
- Текст переносится некрасиво

**Решение:**
```css
@media (max-width: 768px) {
  .card-content {
    flex-direction: column-reverse;  /* Изображение внизу */
  }
  
  .content-right img {
    max-width: 100%;
    max-height: 250px;
  }
}
```

### 3.2 Story Screen (История персонажа)

**Проблемы:**
- Большое изображение персонажа
- Вопросы могут быть в виде bulleted list

**Решение:**
```css
@media (max-width: 480px) {
  .story-content {
    padding: var(--space-md);
  }
  
  .story-content img {
    width: 150px;  /* Вместо 200px */
    margin: 0 auto var(--space-lg);
  }
  
  .questions-list {
    padding-left: var(--space-lg);
    font-size: 14px;
  }
}
```

### 3.3 Character Card Screen (Карточки персонажей)

**Проблемы:**
- Много карточек в сетке
- Изображения обрезаются

**Решение:**
```css
@media (max-width: 480px) {
  .cards-grid {
    grid-template-columns: 1fr;  /* Одна колонка */
    gap: var(--space-md);
  }
  
  .character-card {
    text-align: center;
  }
}

@media (max-width: 768px) and (min-width: 481px) {
  .cards-grid {
    grid-template-columns: repeat(2, 1fr);  /* Две колонки */
  }
}
```

### 3.4 Progress Screen (Прогресс)

**Проблемы:**
- Шкала прогресса может быть слишком узкая
- Числа и процент

**Решение:**
```css
@media (max-width: 480px) {
  .progress-item {
    flex-direction: column;
    gap: var(--space-md);
  }
  
  .progress-bar {
    width: 100%;
  }
  
  .progress-text {
    font-size: 12px;
  }
}
```

### 3.5 Quiz Screen (Тест)

**Проблемы:**
- Кнопки ответов должны быть полной ширины
- Вопрос и варианты должны быть читаемы

**Решение:**
```css
@media (max-width: 480px) {
  .quiz-question {
    font-size: 16px;
    margin-bottom: var(--space-lg);
  }
  
  .quiz-options {
    flex-direction: column;
    gap: var(--space-md);
  }
  
  .quiz-option {
    width: 100%;
    padding: var(--space-md);
  }
}
```

---

## 🖼️ Фаза 4: Оптимизация изображений

### 4.1 Размеры изображений

| Устройство | Размер | Качество |
|-----------|--------|----------|
| Мобильная | 100% ширины | 70-80% |
| Фабллет | 90% ширины | 75-85% |
| Планшет | 80% ширины | 80-90% |
| Десктоп | 600px макс | 90-100% |

### 4.2 Формат изображений

```html
<!-- Использовать picture для разных размеров -->
<picture>
  <source media="(max-width: 480px)" srcset="image-small.webp">
  <source media="(max-width: 768px)" srcset="image-medium.webp">
  <img src="image-large.webp" alt="Description">
</picture>
```

### 4.3 Lazy Loading

```html
<!-- Уже используется loading="lazy" -->
<img src="image.png" alt="Description" loading="lazy">
```

---

## ⚡ Фаза 5: Производительность

### 5.1 Минимизация CSS

```bash
# Использовать CSS минификацию
npm install cssnano --save-dev
```

### 5.2 Сжатие изображений

```bash
# Сжать PNG/JPEG
npm install imagemin-cli --save-dev
imagemin assets/images/**/*.{jpg,png} --out-dir=assets/images
```

### 5.3 Кэширование браузера

```html
<!-- В index.html -->
<meta http-equiv="Cache-Control" content="public, max-age=31536000">
```

### 5.4 Оптимизация шрифтов

```css
/* Использовать system fonts для мобильной версии */
@media (max-width: 480px) {
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  }
}
```

---

## 🧪 Фаза 6: Тестирование

### 6.1 Ручное тестирование

**Чек-лист:**
- [ ] Протестировать на реальных устройствах (iPhone, Android)
- [ ] Проверить все ориентации (Portrait/Landscape)
- [ ] Проверить все экраны на 320px, 375px, 414px, 480px, 768px
- [ ] Проверить сенсорные взаимодействия
- [ ] Проверить производительность (DevTools Performance)

### 6.2 Браузеры для тестирования

- ✓ Chrome Mobile (Android)
- ✓ Safari Mobile (iOS)
- ✓ Firefox Mobile
- ✓ Samsung Internet

### 6.3 Инструменты

```bash
# Chrome DevTools
# F12 → Toggle device toolbar (Ctrl+Shift+M)

# Lighthouse
# F12 → Lighthouse → Generate report

# WebPageTest
# https://www.webpagetest.org/
```

---

## 📋 Фаза 7: Верстка мобильных особенностей

### 7.1 Touch-friendly элементы

```css
/* Минимум 44x44px для кнопок */
button {
  min-width: 44px;
  min-height: 44px;
}

/* Достаточный промежуток между элементами */
a, button {
  margin: var(--space-md);
}
```

### 7.2 Ввод текста на мобильной

```css
/* Предотвратить автозум при фокусе */
input {
  font-size: 16px;  /* 16px минимум */
}
```

### 7.3 Viewport для разных экранов

```html
<meta name="viewport" 
      content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

---

## 🎬 Фаза 8: Конечные оптимизации

### 8.1 Dark mode поддержка (опционально)

```css
@media (prefers-color-scheme: dark) {
  :root {
    --bg-primary: #1a1a1a;
    --text-primary: #ffffff;
  }
}
```

### 8.2 High DPI дисплеи

```css
@media (min-device-pixel-ratio: 2) {
  img {
    /* Использовать 2x изображения */
  }
}
```

### 8.3 Сохранение данных

```html
<meta name="theme-color" content="#163F6F">
```

---

## 📊 Метрики успеха

| Метрика | Целевое значение |
|---------|-----------------|
| Page Load Time (3G) | < 5 сек |
| Lighthouse Score | > 90 |
| Time to Interactive | < 3 сек |
| Cumulative Layout Shift | < 0.1 |
| First Contentful Paint | < 1.8 сек |

---

## 📝 Приоритет задач

### 🔴 Высокий приоритет (Фаза 1-2)
- [ ] Адаптировать контейнеры и текст
- [ ] Оптимизировать размер изображений
- [ ] Сделать кнопки touch-friendly
- [ ] Адаптировать навигацию

### 🟡 Средний приоритет (Фаза 3-4)
- [ ] Адаптировать все типы экранов
- [ ] Оптимизировать макеты
- [ ] Добавить поддержку разных разрешений

### 🟢 Низкий приоритет (Фаза 5-8)
- [ ] Оптимизировать производительность
- [ ] Добавить dark mode
- [ ] Добавить advanced features

---

## 📚 Рекомендации для будущих обновлений

1. **Использовать CSS Grid для мобильных**
   ```css
   @media (max-width: 480px) {
     .grid {
       grid-template-columns: 1fr;
     }
   }
   ```

2. **Flexbox для гибкости**
   ```css
   .flex-container {
     display: flex;
     flex-wrap: wrap;
     gap: var(--space-md);
   }
   ```

3. **Mobile-first подход**
   - Начинать с мобильных стилей
   - Расширять для больших экранов

4. **Accessibility**
   - `font-size >= 16px` для фокуса
   - `contrast ratio >= 4.5:1`
   - `line-height >= 1.5`

---

**Версия плана:** 1.0  
**Дата создания:** 2025-11-06  
**Статус:** Готово к внедрению
